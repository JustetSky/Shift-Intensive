package ru.shift.userimporter.core.model;

public enum ProcessingErrorCode {
    INVALID_FORMAT,
    INVALID_NAME,
    INVALID_LAST_NAME,
    INVALID_MIDDLE_NAME,
    INVALID_EMAIL,
    INVALID_PHONE,
    INVALID_BIRTHDATE
}
